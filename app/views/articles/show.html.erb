<div class="container">
  <div class="container-fluid">

    <!-- Ici on affiche l'article consulté -->
        <div class="jumbotron">
          <p class="text-center">
            <%= image_tag @article.image %>
          </p>

          <h1 class="text-center">
            <%= @article.title %>
          </h1>

          <p class="text-center">
            <%= @article.content %>
          </p>

          <p class="text-right">
            <a href="/">Revenir à l'accueil</a>
          </p>

        </div>
  </div>
</div>